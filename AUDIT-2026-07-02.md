# AUDIT 2026-07-02 — RénoBoost (renoboost-leads)

Source unique de vérité du chantier « audit → patch failles → optimisation ».
Périmètre : dashboard Next.js (`web/`) + pipeline Python (`src/`, `worker/`, `app.py`)
+ base Supabase (advisors) + dépendances (npm audit / pip-audit).

Légende statut : ✅ corrigé (+commit) · ⏭ reporté (+raison) · ❌ non reproduit · ⏸ attente validation Paul.

Méthode : audit cadré sur les **surfaces à risque** (routes API, webhooks, middleware,
migrations/RLS, secrets, PII), pas de scan ligne à ligne. 3 sous-agents parallèles
(sécu web / sécu Python / graphiste) + advisors Supabase + npm/pip audit.

---

## 🔒 Sécurité

| ID | Gravité | Titre | Fichier | Statut |
|----|---------|-------|---------|--------|
| S1 | Critique | RLS `USING (true)` sur ~20 tables (dont `leads`, `lead_contacts`, `phone_numbers`) | Supabase / migration `0001` | ⏸ migration déposée, non appliquée |
| S2 | Élevée | Dump DB avec PII committé dans git | `backups/renoleads_icestream_full_dump.json` | ⏸ retiré du suivi, purge historique = validation Paul |
| S3a | Moyenne | Auth Streamlit fail-open (PII exposée si `APP_PASSWORD` absent) | `app.py:195` | — |
| S3b | Moyenne | SSRF dans le scraper L3 (URL externes non filtrées) | `src/renoboost_leads/stage3_contacts/scraper.py:421` | — |
| S3c | Moyenne | `robots.txt` lu sans timeout → blocage worker possible | `src/renoboost_leads/stage3_contacts/scraper.py:397` | — |
| S3d | Moyenne | Secrets webhook/cron acceptés en query string (fuite via logs) | `web/app/api/{webhooks/instantly,twilio/callback,agent/tick,veille/tick,bornes/ingest}` | ⏸ touche auth entrante |
| S3e | Moyenne | `bornes/ingest` (job 300 s) déclenchable par tout user connecté | `web/app/api/bornes/ingest/route.ts:22` | ⏸ touche auth/rôle |
| S4a | Faible | Comparaison de secret non constante (timing side-channel) | `twilio/callback`, `webhooks/instantly`, `agent/tick` | — |
| S4b | Faible | Lookup lead par `ilike(email)` → injection de motif LIKE | `web/app/api/webhooks/instantly/route.ts:65` | — |
| S4c | Faible | Détails d'erreur serveur renvoyés au client | `bornes/ingest:38`, `twilio/voicemail:57`, `analyse/run:72` | — |
| S4d | Faible | SMTP veille sans timeout | `src/renoboost_leads/veille_immatriculations/mailer.py:143` | — |
| S4e | Faible | Path traversal via `session_id` (outils agent) | `src/renoboost_leads/agent/tools/{sessions,notify,cold_mail}.py` | — |
| S4f | Faible | PII possible dans canaux de diagnostic (`runs.erreur`, heartbeat) | `src/renoboost_leads/stage3_5_enrichment/client.py:126,189` | — |
| S5a | Info | Webhooks Twilio/Instantly sans signature HMAC native | `twilio/callback`, `webhooks/instantly` | ⏭ |
| S5b | Info | Aucune validation Zod sur entrées externes | `web/app/api/lead/*`, `webhooks/*` | ⏭ |
| S5c | Info | Fonction SQL `search_path` mutable + `is_admin()` exécutable + leaked-pwd off | Supabase advisors | ⏸ (via migration S1) |
| DEP | Modéré | postcss XSS (bundlé dans next) — 3 alertes npm | `web/` | ⏭ |

### Détail des correctifs

#### S1 — RLS permissive (advisor Supabase, ~20 tables)
Toutes les policies `*_all_auth` sont `FOR ALL … USING (true) WITH CHECK (true)` :
tout compte authentifié lit/écrit **toutes** les lignes, tous clients confondus
(`campaigns.client_nom`). Advisor : `rls_policy_always_true` × 18 + `is_admin()`
SECURITY DEFINER exécutable + 2 fonctions `search_path` mutable + leaked-password off.
**Correctif** : migration `web/supabase/migrations/0046_*.sql` restreignant les
policies d'écriture (INSERT/UPDATE/DELETE) à un périmètre (`created_by` / rôle admin),
`SET search_path = ''` sur les 2 fonctions, `REVOKE EXECUTE … is_admin` de `authenticated`.
**JAMAIS appliqué en prod sans validation Paul** (contrainte cadrage : touche RLS/données).
**Test** : 2 users distincts → `select count(*) from leads` renvoie chacun son périmètre.

#### S2 — Dump PII dans git
`backups/renoleads_icestream_full_dump.json` (271 Ko, tracké) contient 188 entreprises
avec téléphones/emails de dirigeants (PII RGPD). **Correctif** : `git rm --cached`,
`backups/*.json` ajouté au `.gitignore`. **Purge de l'historique** (`git filter-repo` +
force-push) = action destructive → validation Paul explicite requise.
**Test** : `git ls-files backups | grep -c json` = 0.

#### S3a — Streamlit fail-open
`app.py:195` : si `APP_PASSWORD` absent (ou lecture en échec, `except: return None`),
l'app démarre **ouverte**. **Correctif** : fail-closed par défaut, échappatoire explicite
`APP_ALLOW_OPEN=1` pour le dev. **Test** : `_require_auth` env vide → `st.stop()`.

#### S3b — SSRF scraper
`scraper.py:421` `session.get(url, allow_redirects=True)` où `url` vient de `lead.site_web`
(hostile). Un `http://169.254.169.254/…` est fetché depuis le worker porteur de
`service_role`. **Correctif** : résoudre l'hôte, rejeter IP privée/loopback/link-local/reserved,
schéma `http/https` uniquement. **Test** : `_construire_base_url("http://127.0.0.1")` → `None`.

#### S3c — robots.txt sans timeout
`scraper.py:397` `rp.read()` (`urlopen` sans timeout) fige le thread sur un serveur
tarpit ; heartbeat continue → run « vivant mais mort ». **Correctif** : fetch via
`session.get(robots_url, timeout=…)` puis `rp.parse(...)`. **Test** : mock `Timeout` → pas de blocage.

#### S3d — Secrets en query string
`req.nextUrl.searchParams.get("secret")` : le secret finit dans les access logs Vercel /
Referer. **Correctif** : n'accepter que via header (`Authorization: Bearer` / `x-webhook-secret`).
⏸ Touche l'auth des webhooks entrants (Twilio/Instantly/cron) → validation Paul (risque de
casser les intégrations si l'appelant envoie encore en query). **Test** : `?secret=` → 401 ; header → 200.

#### S3e — bornes/ingest ouvert
Tout user connecté peut relancer une ingestion IRVE de 300 s. **Correctif** : réserver aux
admins + throttle (refuser si ingestion `< N min`). ⏸ Touche rôle/auth → validation Paul.

#### S4a — Comparaison de secret non constante
Réutiliser le helper `safeEqual` (crypto.timingSafeEqual) déjà présent dans
`calendly/webhook/route.ts` pour toutes les comparaisons de secret partagé.

#### S4b — `ilike(email)` → `eq(email)`
`webhooks/instantly/route.ts:65` : `%` dans l'email matcherait un lead arbitraire.
**Correctif** : `.eq("contact_email", email)`. **Test** : payload `email:"%"` → `matched:false`.

#### S4c — Détails d'erreur au client
Renvoyer un message générique client, logger le détail serveur. **Test** : erreur → réponse sans stack.

#### S4d — SMTP sans timeout
`mailer.py:143` `smtplib.SMTP(host, port)` → ajouter `timeout=20`. **Test** : `grep SMTP( | grep -v timeout` vide.

#### S4e — Path traversal `session_id`
Helper `_dossier_session` : `resolve()` + `is_relative_to(OUTPUT_ROOT)` sinon `ValueError`.
**Test** : `read_session("../..")` → erreur, pas de lecture disque.

#### S4f — PII dans diagnostics
`client.py:126,189` : `resp.text[:200]` (corps Dropcontact) peut écho­rer prénom/nom du
dirigeant dans `runs.erreur`/heartbeat. **Correctif** : ne garder que `status_code` + champ
`error`/`reason` JSON. **Test** : mock 400 avec « Jean Dupont » → message sans « Dupont ».

#### S5a/S5b/DEP — reportés
- S5a HMAC natif Twilio/Instantly : le secret partagé fail-closed est acceptable ; aligner
  sur le HMAC Calendly = amélioration, non bloquant.
- S5b Zod : les routes valident manuellement + RLS/secret en amont ; hardening progressif.
- DEP postcss : le seul `npm audit fix` disponible force `next@9.3.3` (downgrade majeur
  cassant, faux positif de plage). Surface = XSS au stringify CSS (build), pas exploitable
  runtime user. Reporté jusqu'à un `next@15.x` patché.

---

## 🎨 UX / Optimisation du dashboard (agent graphiste)

Points forts constatés : pattern Contexte→Actions installé (`ActionsBand`), clics tracés,
`run.erreur` affiché, palette 100 % slate, `--muted` passe AA. Backlog = patchs minimaux.

| ID | Impact × Effort | Titre | Fichier(s) | Statut |
|----|-----------------|-------|-----------|--------|
| U1 | fort × faible | **TOP** Erreurs d'actions avalées en silence (5 composants) | `kanban-card`, `lead-status-actions`, `veille-list`, `lead-relance`, `references-chantiers` | — |
| U2 | fort × faible | **TOP** Sidebar hover-only (inutilisable clavier/tactile) | `components/nav.tsx` | — |
| U3 | fort × faible | **TOP** Aucun `loading.tsx`/`error.tsx` (écran figé/blanc) | `app/(app)/` | — |
| U4 | moyen × faible | Favicon + titre d'onglet manquants | `app/layout.tsx`, `app/icon.svg` | — |
| U5 | moyen × faible | Tables sans scroll horizontal mobile (6 tables) | `suivi`, `tableau-de-bord`, `agent`, `recherche/[id]`, `run-report`, `solaire-workspace` | — |
| U6 | moyen × faible | Composant `Stat` dupliqué 6× | 6 fichiers | ⏭ |
| U7 | moyen × faible | En-têtes de tri non accessibles (`th onClick`) | `components/leads-table.tsx:320` | — |
| U8 | moyen × faible | Checkboxes sans label + boutons kanban < 44px | `leads-table.tsx`, `kanban-card.tsx` | — |
| U9 | moyen × faible | Aucun `focus-visible` (nav clavier à l'aveugle) | `app/globals.css` | — |
| U10 | moyen × faible | Feedback d'action non annoncé (`aria-live`) et éphémère | `leads-table.tsx` + composants | — |
| U11 | faible × faible | Accents bleus incohérents avec la marque verte | `leads-table`, `inbox`, `tableau-de-bord`… | ⏭ |
| U12 | faible × faible | Kanban suivi : 7 colonnes écrasées sur laptop | `app/(app)/suivi/page.tsx:260` | — |

---

## 📊 Rapport final
_(complété en fin de chantier : fermées / restantes / livrées)_
