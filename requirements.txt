# Requirements pour Streamlit Cloud (https://share.streamlit.io)
# Streamlit Cloud lit ce fichier pour installer les dépendances de l'app.
# Le projet lui-même est installé en éditable via setup.py / pyproject.toml.
#
# Pour le développement local, préfère :
#   pip install -e ".[dev,ui]"
# qui pioche les deps dans pyproject.toml.

# Core pipeline
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
requests>=2.31.0
tenacity>=8.2.3
pandas>=2.1.0
click>=8.1.7
rich>=13.7.0
anthropic>=0.39.0
jinja2>=3.1.2
beautifulsoup4>=4.12.0
lxml>=5.0.0

# UI
streamlit>=1.30.0

# Le package lui-même (Streamlit Cloud le détecte via pyproject.toml et l'installe)
-e .
